export const createEmail = (req, res) => {
    const { msg } = req.params;
    const time = new Date().toISOString();
    // create schema instance push in to db
}

export const fetchUnreadMessages = (req, res) => {
    // use queary to fetch the response
    // res.send(response)
}

